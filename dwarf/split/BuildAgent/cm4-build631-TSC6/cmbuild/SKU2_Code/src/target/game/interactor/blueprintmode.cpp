/*
    Compile unit: C:\BuildAgent\cm4-build631-TSC6\cmbuild\SKU2_Code\src\target\game\interactor\blueprintmode.cpp
    Producer: MW EABI PPC C-Compiler
    Language: C++
    Code range: 0x80080020 -> 0x80080378
*/
// Range: 0x80080020 -> 0x800801A4
void BlueprintInteractor::EnterBlueprintMode(class BlueprintInteractor * const this /* r30 */, unsigned char bReapply /* r31 */) {
    // References
    // -> class BluePrintGrid * s_pBPG;
    // -> class HeightFade g_heightFade;
    // -> unsigned char m_blueprintMode;
    // -> unsigned char m_lastBlueprintMode;
    // -> class EVec3 m_vUp;
    // -> float m_lastRotAng;
    // -> float m_lastZoom;
    // -> float m_lastTilt;
    // -> float m_lastMaxZoom;
    // -> unsigned int m_lastCamMode;
}

// Range: 0x800801A4 -> 0x80080284
void BlueprintInteractor::ExitBlueprintMode(class BlueprintInteractor * const this /* r31 */) {
    // References
    // -> class BluePrintGrid * s_pBPG;
    // -> class HeightFade g_heightFade;
    // -> unsigned char m_blueprintModeChangeInteractor;
    // -> unsigned char m_blueprintMode;
    // -> unsigned char m_lastBlueprintMode;
    // -> class EVec3 m_vUp;
    // -> float m_lastZoom;
    // -> float m_lastTilt;
    // -> float m_lastMaxZoom;
    // -> unsigned int m_lastCamMode;
}

// Range: 0x80080284 -> 0x800802F8
void BlueprintInteractor::SetBlueprintModeInteractorChanging(class BlueprintInteractor * const this /* r31 */) {
    // References
    // -> class BluePrintGrid * s_pBPG;
    // -> unsigned char m_blueprintModeChangeInteractor;
    // -> class HeightFade g_heightFade;
}

// Range: 0x800802F8 -> 0x80080330
void BlueprintInteractor::CheckAndReapplyBlueprintMode() {
    // References
    // -> unsigned char m_blueprintModeChangeInteractor;
}

// Range: 0x80080330 -> 0x80080378
void BlueprintInteractor::BlueprintNotifyQuittingBuildbuy() {
    // References
    // -> unsigned char m_blueprintModeChangeInteractor;
    // -> unsigned char m_lastBlueprintMode;
    // -> class HeightFade g_heightFade;
    // -> unsigned char m_blueprintMode;
}


